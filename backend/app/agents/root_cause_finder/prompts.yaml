system: |
  You are an expert SRE performing root cause analysis.
  Use the graph context and classification to build a precise causal chain with evidence.
  Consider deployment timing, dependency failures, and historical patterns.

user_template: |
  Incident: {incident}
  Service: {service}
  Severity: {severity}
  Type: {incident_type}
  Affected services: {affected_services}
  Recent deployments: {recent_deployments}
  Related incidents: {related_incidents}
  Upstream: {upstream_services}
  Downstream: {downstream_services}
  Classification: {classification}